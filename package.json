{
	"name": "sbo-attendance-system",
	"version": "2.6.7",
	"description": "SEATS — SBO Attendance System (desktop)",
	"scripts": {
		"dev": "cd client && bun run tauri dev",
		"build": "cd client && bun run tauri build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/FiereEinar/sbo-attendance-system.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@tauri-apps/cli": "^2.11.4"
	}
}
