{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splashscreen",
  "description": "Event-only capability for the static startup splash window",
  "windows": ["splashscreen"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
